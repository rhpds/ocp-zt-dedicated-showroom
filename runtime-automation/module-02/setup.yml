---
- name: Module 2 Setup
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Setup complete
      ansible.builtin.debug:
        msg: "Log in as developer user and create a ConfigMap in dev-project."
